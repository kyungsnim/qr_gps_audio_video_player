name: qr_gps_audio_video_player
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"
dependencies:
  flutter:
    sdk: flutter

  qr_code_scanner: ^0.6.1
  geolocator: ^7.7.1
  cupertino_icons: ^1.0.2
  url_launcher: ^6.0.14

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0

flutter:

  uses-material-design: true